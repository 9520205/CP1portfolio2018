// setup runs once
void setup() {
  size(600,600);
  background(255);
  frameRate(60);
}

// draw runs on a loop
void draw () {
  //background(255);
  //line(0,0,mouseX,mouseY);
  fill(180,245,random(255));
  ellipse(mouseX,mouseY,random(30,550),random(30,550));
}



// setup runs once
void setup() {
  size(1000,1000);
  background(255);
  frameRate(60);
}

// draw runs on a loop
void draw () {
  //background(255);
  //line(0,0,mouseX,mouseY);
  fill(random(0,255),random(0,255),random(0,255));
  ellipse(mouseX,mouseY,random(0,1000),random(0,1000));
}



size(2560,1394);
for (int x=0; x < 2560; x+=80) {
    for (int y = 0; y < height; y+=80) {
     fill(random(255), random(255), random(255));
     stroke(random(255));
     rect(x,y,80,80);
    }
}
